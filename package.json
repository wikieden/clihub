{
  "name": "clihub-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "clihub — cross-platform AI CLI toolkit manager",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bash scripts/test-install.sh",
    "cli": "bun packages/cli/src/cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
