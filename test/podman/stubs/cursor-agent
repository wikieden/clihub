#!/bin/sh
# Stub Cursor CLI for clihub's test container. Answers --version only.
case "$1" in
  --version|-v|version) echo "2026.05.20 (cursor-agent stub)" ;;
  *) echo "stub cursor-agent — not the real CLI"; exit 0 ;;
esac
